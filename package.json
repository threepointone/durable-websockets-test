{
  "name": "durable-websockets-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.2.12",
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.0.4",
    "vitest": "1.5.0",
    "wrangler": "^3.0.0"
  }
}
